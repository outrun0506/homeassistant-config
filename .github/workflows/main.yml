name: "Check HA Config"
on:
  push:
    branch:
      - master
  pull_request:
    branch:
      - master
jobs:
  check:
    - name: 👷 Setup Home Assistant
    id: homeassistant
    uses: ludeeus/setup-homeassistant@main
